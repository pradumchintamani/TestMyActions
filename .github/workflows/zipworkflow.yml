on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: self-hosted

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
      - name: action-zip
        uses: montudor/action-zip@v1.0.0
